using System;
using System.IO;
using System.Text;
using

public class Server
{
    public static void ProcessAuthorization(string requestsPath, string requestsResultsPath)
    {
        
        using (StreamReader sr = new StreamReader(File.OpenRead(requestsPath), Encoding.UTF8)
        {

        }
    }
}